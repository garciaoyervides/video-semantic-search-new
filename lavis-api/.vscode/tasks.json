{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "flask server",
            "type": "shell",
            "command": "conda activate D:/conda-envs/video-search ; python -m flask run --debugger -p 5002"
        },
        {
            "label": "flask server no reload",
            "type": "shell",
            "command": "conda activate D:/conda-envs/video-search ; python -m flask run --debugger --no-reload -p 5002"
        }
    ]
}