{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "flask server",
            "type": "shell",
            "command": "conda activate D:/conda-envs/audio-processing ; python -m flask run --debugger -p 5001"
        },
        {
            "label": "flask server no reload",
            "type": "shell",
            "command": "conda activate D:/conda-envs/audio-processing ; python -m flask run --debugger --no-reload -p 5001"
        }
    ]
}